import { Routes } from '@angular/router';
import { ConverterComponent } from '../features/converter/converter.component';

export const routes: Routes = [
    { path: 'dash', component: ConverterComponent },
];
